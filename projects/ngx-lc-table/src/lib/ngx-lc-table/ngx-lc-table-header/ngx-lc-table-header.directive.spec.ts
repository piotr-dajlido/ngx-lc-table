import { NgxLcTableHeaderDirective } from './ngx-lc-table-header.directive';

describe('NgxLcTableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxLcTableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
